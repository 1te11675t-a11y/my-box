# 引越し箱管理ツール 構成検討

要件定義書（`moving-box-manager_requirements.md`）を受けて、**実装前に決めておく構成**をまとめたもの。
既存の `プライベートタスク_モバイル/` と同じ骨格で作ることを前提にしている。

---

## 0. 結論（先に3行）

1. **既存のタスクアプリと同じ「1フォルダのPWA」構成**にする。GitHub Pages はもう使えているので、設計判断2は「https配信」で確定。
2. **QRの中身はプレーンな箱IDではなく、アプリのURL**（`https://…/boxes/#B012`）にする。iPhone標準カメラで読むだけで該当箱が開くので、**アプリ内スキャナが無くても運用が成立する**。
3. **ラベルは先に一括印刷して箱に貼る**（データ不要・IDだけで作れる）。これで「印刷はPC・データはiPhone」という localStorage の分断問題が消える。

---

## 1. 既存アプリから流用する骨格

`プライベートタスク_モバイル/index.html`（1607行）で既に確立している型をそのまま使う。ゼロから考え直す必要はない。

| 部品 | 流用元 | 箱アプリでの使い道 |
|---|---|---|
| CSS変数のダークテーマ（`--bg` `--panel` `--accent` …） | 冒頭の `:root` | 見た目を揃える。優先度の色だけ差し替え |
| `appbar` + `main` + `tabbar` + `#fab` のレイアウト | 同上 | そのまま。`body { height:100dvh; overflow:hidden }` でタブ位置が浮かないやつ |
| セーフエリア対応（`env(safe-area-inset-*)`） | 同上 | そのまま |
| 入力欄16px以上（Safariの自動ズーム防止） | 同上 | そのまま |
| ボトムシート `openSheet(build)` / `closeSheet()` | 1090行付近 | 箱の詳細・編集・データ画面すべてこれ |
| `normalizeTask()` 方式の正規化 | 430行付近 | `normalizeBox()`。古い形のJSONを読んでも壊れない |
| `snapshot()` / `commit(before, label)` / `undo()` + トースト | 608〜660行 | 削除の取り消し。引越し中の誤操作対策として効く |
| `exportFile()` / インポート | 1370行付近 | JSONバックアップ。形式もそっくり真似る |
| `sw.js` / `manifest.webmanifest` / アイコン3枚 | フォルダごと | 中身のURLと名前を書き換えるだけ |

> **メモ:** 要件定義の「単一HTMLファイル」は、PWA化した時点（manifest・sw.js・アイコンが要る）で既に崩れている。
> 守るべき制約は **「1フォルダで完結・実行時に外部へ通信しない」** と読み替えるのが正しい。
> そのほうがQRライブラリを別ファイルに置けて、index.html が読みやすいままになる。

---

## 2. 設計判断2（配信方式）の確定 → **https配信**

要件定義では「推奨だが難しければ file:// にフォールバック」となっていたが、**すでに `https://1te11675t-a11y.github.io/my-task/` が動いている**ので迷う理由がない。https で確定。

- カメラ（getUserMedia）が使える
- localStorage がオリジン単位で安定して残る（`file://` は端末・ブラウザによって挙動が怪しい）
- ホーム画面に追加して全画面アプリになる
- Service Worker でオフライン動作

### 置き場所：新しいリポジトリを作るのを推奨

| 案 | URL | 評価 |
|---|---|---|
| **A. 新リポジトリ `my-boxes`（推奨）** | `https://1te11675t-a11y.github.io/my-boxes/` | Service Worker のスコープがタスクアプリと完全に分かれる。URLも短くQRが小さくなる |
| B. 既存リポジトリのサブフォルダ | `https://1te11675t-a11y.github.io/my-task/boxes/` | リポジトリ作成の手間なし。ただしタスクアプリの sw.js（スコープ `/my-task/`）が箱アプリのファイルもキャッシュしにいくので、更新が反映されない事故が起きうる |

リポジトリ作成は一度やっているので、Aで問題ないはず。

---

## 3. 設計判断1（QRの中身）→ **ID単体ではなくURLにする**

要件定義は「QR＝プレーンテキストの箱ID（`B012`）」を採用としていたが、**URL方式に変えることを提案する**。

```
https://1te11675t-a11y.github.io/my-boxes/#B012
```

理由：

- **iPhoneの標準カメラで読むと、そのままアプリが開いて該当箱の詳細が出る。** プレーンテキスト `B012` だと「B012という文字列です」と出るだけで、そこから手作業でアプリに入力しなおすことになる。
- つまり **アプリ内スキャナが必須ではなくなる**。MVPは標準カメラ運用で出して、jsQR（後述）は後から足せばいい。実装リスクがいちばん減る。
- 家族に箱を探してもらうときも、URLを読めばその人の端末でもアプリが開く（データは入っていないのでIDと部屋名しか出ないが、それでも十分なことが多い）。

デメリットと対策：

- **印刷済みラベルにホスト名が焼き付く。** 将来リポジトリ名を変えたり会社版に移すと、古いラベルは古いURLを指す。
  → 対策：読み取り処理を**両対応**にする。`#` 以降があればそれをID、無ければ文字列全体をIDとして扱う。さらにホストが違っても `#B012` の部分だけ見る。これなら後からID方式のQRを混ぜても動く。
- QRが少し複雑になる（URLは40文字前後）。→ 誤り訂正レベルMで 25×25 マス程度。3cm角に印刷すれば標準カメラで問題なく読める。

### 読み取り処理の方針（擬似コード）

```js
// QRの中身（またはURLのハッシュ）から箱IDを取り出す。どの形式でも通す。
function boxIdFrom(text) {
  const s = String(text).trim();
  const hash = s.includes("#") ? s.slice(s.lastIndexOf("#") + 1) : s;
  const id = decodeURIComponent(hash).trim().toUpperCase();
  return /^[A-Z0-9-]{1,16}$/.test(id) ? id : "";
}
```

---

## 4. 運用フローの提案 ＝ **ラベル先行印刷**

ここが構成にいちばん効くので、先に決めておきたい。

### 問題

localStorage は端末単位。**ラベル印刷はPCでやりたいが、データはiPhoneの中にある。**
「箱を登録 → その箱のQRを印刷」という順番だと、毎回 iPhone → JSON書き出し → PCへ送る → PCで読み込む → 印刷、が必要になる。引越し中にやりたくない。

### 解決：QRラベルはデータに依存しない

QRの中身は `#B012` だけ。**箱の中身を知らなくてもラベルは作れる。**

1. **引越し前に、PCで `B001〜B060` のQRラベルをA4に一括印刷**（アプリを開いて「ID範囲を指定して生成」するだけ。データ0件でよい）
2. 段ボールに貼る
3. 詰めながら iPhone でQRを読む → アプリが開く → **未登録IDなら「新規登録」シートが出る** → 部屋と中身を入力して保存
4. 開梱時も同じ。読む → 詳細が出る → 開梱済みをタップ

この順番なら、PCとiPhoneの間でデータをやり取りする場面が一度も無い。

### 構成への影響

- ラベル印刷画面は「**登録済みの箱から選ぶ**」ではなく「**ID範囲を入力して生成**」を主にする（登録済みからの再印刷も選べるようにはする）
- スキャン/ハッシュ遷移で**未登録IDに来たときの新規登録フロー**が主要導線になる。ここを丁寧に作る
- 自動採番（`B001…`）は「次の空き番号」ではなく「印刷済みの通し番号を人間が貼った結果」に従う。**手入力優先**でよい

---

## 5. 画面構成

タスクアプリは5タブだが、箱アプリはやることが少ないので **3タブ + 右上⚙ + 右下＋** で足りる。

| 位置 | 名前 | 中身 |
|---|---|---|
| タブ1 | 🗂 **箱** | 検索バー（ID・部屋・中身を横断）＋フィルタ（部屋 / 未開梱・開梱済 / 優先度）＋箱カード一覧。要件の F-2・F-3 をここ1画面に統合 |
| タブ2 | 📷 **スキャン** | アプリ内カメラ。**MVPでは「標準カメラで読んでください」の案内＋ID直接入力欄**でも可（後述の段階実装） |
| タブ3 | 🖨 **ラベル** | ID範囲（`B001` 〜 `B060`）を指定してA4に並べる。印刷プレビュー |
| 右上 ⚙ | データ | 書き出し・読み込み・全消去・保存状況（タスクアプリの `openDataSheet` と同じ） |
| 右下 ＋ | 箱を追加 | 新規登録シート |
| （シート） | 箱の詳細 | 箱情報・アイテム・開梱トグル・QR表示・編集・削除 |

要件定義の6画面のうち「登録／編集フォーム」「箱詳細」はボトムシートに、「設定／バックアップ」は⚙に畳んでいる。タスクアプリと同じ流儀。

### 画面遷移は hash で行う

`#B012` で箱詳細を開く。`hashchange` を拾って詳細シートを出す。
これはURL方式QRの受け口そのものなので、**ルーティングとQR読み取りが同じ1本の経路になる**。

---

## 6. データモデル

要件定義の案をベースに、**将来の在庫管理で確実に必要になるところだけ先に器を用意する**。

```json
{
  "version": 1,
  "boxes": [
    {
      "id": "B012",
      "place": "寝室",
      "items": [
        { "name": "冬用毛布", "qty": 1, "note": "" }
      ],
      "priority": "later",
      "opened": false,
      "note": "上に載せない",
      "photo": null,
      "createdAt": "2026-08-31T10:00:00+09:00",
      "updatedAt": "2026-08-31T10:00:00+09:00"
    }
  ]
}
```

要件定義からの変更点は2つだけ。

| 項目 | 要件定義 | 提案 | 理由 |
|---|---|---|---|
| `room` | `"room": "寝室"` | **`"place"`** | 在庫版では「部屋」ではなく「棚」「保管庫」になる。UIのラベルだけ「行き先の部屋」と出せばよく、データ側は場所一般にしておく |
| `items` | `["冬用毛布", …]` 文字列の配列 | **`[{ name, qty, note }]` オブジェクトの配列** | 在庫版では数量・ロット・使用期限が必ず要る。後から型を変えるとJSONの互換が切れる。**文字列も受け付けて `{name: s, qty: 1}` に正規化**すれば入力の手軽さは落ちない |

その他の方針：

- `normalizeBox(raw)` で古い形（`room` / 文字列アイテム）を吸収する。タスクアプリの `normalizeTask()` と同じ考え方
- `id` は大文字英数とハイフンのみ、一意。重複時は保存を止めて警告
- localStorage のキーは `moving_boxes_v1`（タスクアプリの `personal_taskboard_v1` と同じ命名）

---

## 7. 保存層の分離（会社版への差し替え口）

要件定義の「`storage.js` 相当に閉じ込める」を具体化する。**ポイントは最初から `async` にしておくこと。**
localStorage は同期だが、サーバは非同期。ここを同期で書くと、差し替え時に呼び出し側を全部書き直すことになる。

```js
/* 保存層。ここだけ差し替えれば会社版（共有サーバ）になる。
   localStorage は同期だが、将来の fetch に合わせて最初から async にしておく。 */
const Store = {
  async loadAll()        { /* localStorage から全件 */ },
  async saveAll(data)    { /* localStorage へ全件 */ },
  async get(id)          { /* 1件 */ },
  async put(box)         { /* 1件 追加・更新（updatedAt を打つ） */ },
  async remove(id)       { /* 1件 削除 */ },
};
```

- 画面側（`render()` / シート）は `Store` の外の形を一切知らない
- 会社版では `loadAll` を `fetch("/api/boxes")` に、`put` を `fetch(..., {method:"PUT"})` に置き換える
- **同期・排他は今は考えない**（要件どおり「最後の保存が有効」）。ただし `updatedAt` を必ず持たせておくと、後で競合検知に使える

---

## 8. ファイル構成

```
引越し箱管理/
├── index.html              … 本体（HTML + CSS + アプリのJS）
├── qrcode.js               … QR生成ライブラリ（同梱・約20KB）
├── jsqr.js                 … QR読み取りライブラリ（同梱・約170KB／段階2で追加）
├── sw.js                   … オフライン用。上の4ファイルをキャッシュ
├── manifest.webmanifest
├── icon-192.png / icon-512.png / apple-touch-icon.png
└── README.md               … 使い方とGitHub Pagesへの上げ方
```

ライブラリを別ファイルにするのは、index.html を人間が読める長さに保つため。
**実行時に外部へ出ない**という制約は、ファイルを同梱している以上守られている。

---

## 9. ライブラリ

| 用途 | 候補 | サイズ | ライセンス | 備考 |
|---|---|---|---|---|
| QR**生成** | `qrcode-generator`（kazuhikoarase） | 約20KB | MIT | 必須。SVG出力ができるので印刷が綺麗 |
| QR**読み取り** | `jsQR` | 約170KB | Apache-2.0 | 段階2。iOS Safari は `BarcodeDetector` 非対応なのでこれが要る |
| （読み取り高速版） | `BarcodeDetector` API | 0 | ブラウザ標準 | Android Chrome のみ。あれば使い、無ければ jsQR に落とす |

いずれも**一度ダウンロードしてフォルダに置く**（実装時に取得する）。実行時のCDN参照はしない。

---

## 10. 実装の順番

**段階1（スキャナ無しで完成させる）** ― ここまでで要件の受け入れ基準はほぼ埋まる

1. 骨格（タスクアプリのCSS・レイアウト・シート・トースト・undoを移植）
2. `Store` + `normalizeBox` + 一覧・検索・フィルタ
3. 箱の登録・編集・削除、アイテム、開梱トグル
4. hash ルーティング（`#B012` で詳細、未登録なら新規登録）
5. QR生成＋ラベル印刷（ID範囲指定でA4に並べる）
6. エクスポート／インポート
7. PWA化（manifest・sw.js・アイコン）、GitHub Pages へ

→ この時点で「標準カメラでQRを読む → アプリが開く → 編集」が成立する。

**段階2（あれば快適）**

8. jsQR を同梱してアプリ内スキャナ（連続読み取り。箱を次々処理するときは標準カメラより速い）
9. 写真1枚（`photo` に dataURL、長辺512pxに縮小）

**段階3（会社版）**

10. `Store` をサーバ実装に差し替え。ID採番の一元化、複数人の競合、認証をそこで考える

---

## 11. 決定事項（2026-08-31）

| # | 論点 | 決定 |
|---|---|---|
| 1 | QRの中身 | **URL方式**（`https://…/my-boxes/#B012`）。素のIDのQRも読めるよう両対応 |
| 2 | 運用 | **ラベル先行印刷**。ラベル画面は「ID範囲を指定して生成」が主 |
| 3 | 置き場所 | **新リポジトリ `my-boxes`**（Service Worker の干渉を避ける） |
| 4 | 箱IDの体系 | **決めない。自由入力**（下記） |
| 5 | アプリ内スキャナ | **段階2送り**。MVPは標準カメラ運用 |

### 4の詳細：IDの体系は固定しない

人・部屋・時期など、後から好きに分けたいという要望。事前設定を一切要らなくするため、次のようにした。

- IDは16文字まで自由入力。英数でも日本語でも通る（`B012` / `妻-01` / `寝室A`）
- **IDの末尾の数字を落とした部分を「グループ」として自動抽出**する
  （`B012`→`B`、`妻-01`→`妻`、`TOMO-012`→`TOMO`）
- 箱タブのグループ絞り込みは、**実際に使われたIDから勝手に生える**。設定画面もマスタ登録も無い
- ラベル生成の「接頭辞」も自由入力なので、`妻-` と入れれば `妻-001…` が刷れる

在庫版でも同じ仕組みがそのまま効く（`試薬-` `治具-` `試験片-` などで勝手に分類される）。

---

## 12. 実装後のメモ

段階1は実装済み（`index.html` ほか）。実装しながら決めたこと・確かめたことだけ書き足しておく。

- **QRライブラリ**：`qrcode-generator`（MIT）を同梱。URL 47文字で 33×33 モジュール（バージョン4・誤り訂正M）。
  日本語IDを percent-encode しても 33×33 のままだったので、`妻-01` のようなIDでもQRは大きくならない。
- **QRの検証**：生成したモジュール列を jsQR でデコードして、元のURLと一致することを確認した。
- **画面のQRは `<img>` の PNG**。iPhoneで長押し→「写真に追加」ができる。SVGだと長押し保存ができない。
- **印刷ラベルは SVG**。QRも文字も同じ比率で拡大されるので、プレビューと印刷結果がずれない。
  A4・余白8mmで、12面レイアウトのセルが実測 64.7×70.2mm になることを確認済み。
- **QRは必ず白地・黒**で描く。ダークUIのままスクショすると読めないQRになるため、`.qrcard` だけ白くしてある。
- **古い形のJSONも読める**。要件定義に載っていた `room` ＋文字列アイテムの形をそのまま読み込め、
  保存しなおすと `place` ＋ `{name,qty,note}` に移る。
- 検索は、ひらがな／カタカナ・全角／半角・空白の違いを吸収する（`まぐかっぷ` → マグカップ）。
  漢字とかなの相互変換まではしない（`もうふ` では `毛布` は出ない）。
